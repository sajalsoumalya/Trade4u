# Trade4u - Streamlit Web App (Working)
# For Coolify deployment

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - LLM_PROVIDER=opencode
    env_file:
      - .env.full