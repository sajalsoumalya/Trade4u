services:
  tradingagents:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - tradingagents_data:/home/appuser/.tradingagents
    restart: unless-stopped

volumes:
  tradingagents_data: